import React from 'react';
import {useDispatch, useSelector} from "react-redux";

const HomePage = () => {
    return (
        <div>
            <h1>Home page</h1>
        </div>
    );
};

export default HomePage;